# PM Daily Report — 2026-03-25

**Status: YELLOW**
**Live Site Health: UNVERIFIED** (network-restricted environment; UptimeRobot setup remains open action item)

---

## Shipped Since Last Report

Heavy ops activity — 20 commits in last 48 hours. Mostly agent reports and CLAUDE.md updates. No new functional app.jsx changes landed today. The codebase is stable at 6,134 lines.

Previously shipped and holding: HTTPS proxy (Caddy + Let's Encrypt), Plausible analytics (5 custom events), PWA manifest + service worker, JSON-LD structured data (SEO 91%), venue trim to 205 with photos, cache buster v=20260325c.

---

## Bugs Found

**P1 — VenueDetailSheet CTA not sticky.** Photo hero and flight CTA exist in the code (lines ~4820–4910), but the booking buttons scroll off-screen as users read tips and gear sections. This means the primary revenue surface (Book Flights → Travelpayouts, Find Hotels → Booking.com) is invisible once a user scrolls past the hero. Explains zero affiliate conversion. **This is the #1 blocker and has been flagged P1 for 4 consecutive cycles.**

**P2 — 14 duplicate Unsplash photos across 205 venues.** CLAUDE.md claims 100% unique. Actual: ~93%. Ski resorts are the worst offenders (Aspen/Stratton/Stowe sharing photos, Tahoe cluster sharing photos). Minor visual quality hit — not a launch blocker but contradicts our own docs.

**P2 — Venue count drift.** CLAUDE.md says 192 venues. Actual count: 205. Doc is stale by +13 venues. Needs a CLAUDE.md update.

**P2 — Open-Meteo rate limit risk.** ~285 API calls per page load (205 weather + ~80 marine), no localStorage cache. At 30 concurrent users post-Reddit, we blow through the 10K/day free tier in ~70 minutes. Not a launch blocker but a growth blocker.

**P3 — Sentry DSN still empty (line 27).** Zero production error visibility. Jack action, 5 minutes at sentry.io free tier. Still not done.

**P3 — GuidesTab is dead code.** Wired into nothing. BottomNav shows 3 tabs (Explore, Alerts, Profile) as intended. GuidesTab, TripsTab, WishlistsTab all exist but aren't rendered. Correct per decision to keep 3-tab nav, but GuidesTab should either be wired in or removed to reduce code debt.

---

## Top 3 Priorities This Week

### 1. VenueDetailSheet sticky CTA (Dev, 4–6 hrs) — GATES REDDIT LAUNCH
Add a fixed-position bottom bar with "Book Flights $X" and "Find Hotels" that stays visible during scroll. This is the revenue lever. Nothing else matters until this ships. Include score breakdown expandable section and thumbs up/down validation in the same sprint (extra 1–2 hrs).

### 2. Open-Meteo weather cache (Dev, 2 hrs)
Implement localStorage cache keyed by `${lat}_${lon}_${date}` with 30-min TTL. Without this, any Reddit traffic spike kills the weather API and every score drops to zero. Must ship before any growth push.

### 3. Jack actions: Sentry DSN + REI Avantlink (Jack, 35 min total)
Sentry gives us crash visibility before launch. REI Avantlink turns 22 dead links into revenue (no LLC required). Both are 30-min tasks that have been sitting for days.

---

## Decision Made

**Cutting GuidesTab from next sprint scope.** It exists as dead code (not in BottomNav), was briefly mentioned in CLAUDE.md as "wired in" but isn't. We're holding at 3 tabs until 1K users. GuidesTab code stays in app.jsx but is not a priority to wire up or remove. If someone touches BottomNav for another reason, clean it up then.

**Upgrading Open-Meteo cache from P2 → P1.** Previous reports called this P2. With Reddit launch imminent, 30 concurrent users is realistic day-one. Rate limit failure = all scores show zero = users bounce = launch fails. This ships in the same sprint as VenueDetailSheet or immediately after.

---

## Blockers for Jack

1. **Sentry DSN** — sentry.io → create project → paste DSN into app.jsx line 27. 5 minutes. This has been flagged for 3 days.
2. **REI Avantlink signup** — avantlink.com, 30 min, no LLC needed. Turns 22 dead affiliate links into revenue.
3. **LLC status check** — Any update? Blocks Stripe, GetYourGuide, Backcountry, domain. +$21.17 RPM waiting.

---

## Launch Readiness

| Gate | Status |
|------|--------|
| Core app functional | ✅ |
| Photos on all venues | ✅ (93% unique, good enough) |
| Flight API (HTTPS) | ✅ |
| Analytics | ✅ |
| VenueDetailSheet sticky CTA | ❌ BLOCKING |
| Weather cache for scale | ❌ BLOCKING growth |
| Sentry error monitoring | ❌ Jack action |

**Bottom line:** We are one dev sprint away from Reddit launch. Ship sticky CTA + weather cache this week, get Sentry DSN from Jack, and we're go.
