# Peakly PM Report — 2026-04-12

**Filed by:** Product Manager agent
**Status:** 🔴 **RED — P0 syntax error still live on main. Site is still broken.**

---

## TL;DR for Jack

**The line-313 double-comma bug from yesterday's report has NOT been fixed.** Zero commits since the last report (`9777271`, April 11). The site has been serving a blank white screen for ~36+ hours. Every hour this stays broken is lost credibility if anyone hits the URL.

Fix is still one character:

```diff
-    accent:"#40c4a8", tags:["Expert","Offshore Winds"], photo:"https://images.unsplash.com/photo-1526813951498-5498cce49cdf?w=800&h=600&fit=crop&fp-x=0.66&fp-y=0.42",,facing:330},
+    accent:"#40c4a8", tags:["Expert","Offshore Winds"], photo:"https://images.unsplash.com/photo-1526813951498-5498cce49cdf?w=800&h=600&fit=crop&fp-x=0.66&fp-y=0.42", facing:330},
```

File: `app.jsx`, line 313. Delete one comma. Push.

---

## Live Site Health — DOWN (inferred)

Egress restrictions prevent a direct HTTP probe. But the state of `main` is unchanged from yesterday — the double-comma parse error at line 313 means Babel Standalone will throw `SyntaxError: Unexpected token ','` before any React code executes. The ErrorBoundary never mounts. Users see a blank white screen.

**Days broken:** ~1.5 (since commit `266a3c7` deployed April 10 ~11pm).

---

## Commits Since Last Report

**None.** Last commit: `9777271` (April 11). Git log shows zero activity in the last 24 hours.

---

## Bug Triage — 2026-04-12

| Bug | Sev | Status | Days Open |
|---|---|---|---|
| **`,,facing:330` parse error (line 313)** | **P0 — site down** | ❌ STILL OPEN | ~1.5 |
| No CI parse check on deploy | P1 | ❌ Open | 2 |
| No onboarding scoring explanation | P2 | ❌ Open | 8+ |
| Strike alerts server worker | P2 | ❌ Open | 15+ |
| No SRI on CDN scripts / no CSP | P3 | ❌ Open | 15+ |

Everything below the P0 is frozen until the site loads.

---

## Venue Data Verified

| Category | Count |
|---|---|
| Beach (tanning) | 87 |
| Surf (surfing) | 77 |
| Ski/Board (skiing) | 67 |
| **Total** | **231** |

CLAUDE.md says 231. Grep confirms 231 (87+77+67). Accurate.

CATEGORIES array has 4 entries: All, Ski/Board, Surf, Beach. Correct per launch scope.

---

## Deploy Pipeline — No Parse Guard

`.github/workflows/deploy.yml` still deploys raw static files to Pages with zero validation. No `npx babel` parse step, no lint, no smoke test. This is why the double-comma shipped to prod. Adding a parse step remains the #2 priority after the fix itself.

---

## Top 3 Priorities This Week

1. **Fix line 313 and push.** (Jack — 30 seconds.) This is day 2 of a broken site. Nothing else moves until this ships.
2. **Add Babel parse check to CI.** (Dev — 15 min.) Add a step to `deploy.yml` that runs `npx @babel/parser` on `app.jsx` before the upload-artifact step. If it fails, deploy fails. This defect class has occurred twice in 17 days — it's a pattern.
3. **Onboarding scoring explanation.** (Dev — 2-3 hrs.) The scoring algorithm is now correct (post `9777271`). New users need to understand what the numbers mean before Reddit traffic arrives.

---

## Decisions Made (PM)

- **Decision: Holding all feature work until site is green.** The temptation after yesterday's massive cleanup (honest flight pricing, fixed surf scoring, venue dedup) is to keep shipping features. But the site is down. Zero feature work until (a) line 313 is fixed, (b) CI parse check is live, and (c) the fix is verified in an incognito window on mobile.
- **Decision: CLAUDE.md accuracy note.** CLAUDE.md says "231 entries" for VENUES which matches reality (87+77+67=231). However it also says the scoring freeze reference is "PM v16, 2026-03-27" — this should be updated to reference commit `9777271` as the new freeze baseline, per yesterday's report. Bundle this with the line-313 fix commit.

---

## Blockers (need Jack)

1. **Push the one-comma fix on line 313.** This is the only blocker. Day 2.

---

*PM agent — 2026-04-12*
*Filed for: Jack (jjciluzzi@gmail.com)*
*Next milestone: site loads again.*
