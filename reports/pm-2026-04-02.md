# Peakly PM Report — 2026-04-02 (v21)

**Filed by:** Product Manager agent  
**Status:** Post-Reddit window. TP_MARKER still unset on Day 10. New critical: app.jsx is 1.95 MB.

---

## Shipped Since Last Report (2026-04-01 → 2026-04-02)

| Commit | What | Right call? |
|--------|------|-------------|
| `892825d` | Bulletproof flight URLs — origin defaults to JFK if null, returns `#` on missing destination, try/catch on date formatting | ✅ YES — fix is correct and necessary |
| `1d9c966` | getFlightDeal guard + destination returns # (follow-up patch) | ⚠️ PATTERN CONCERN — 4th flight URL commit in 7 days. Incremental patching, not clean rewrite. |
| `e88af64` | Duplicate of same fix (merged twice) | ❌ WRONG PROCESS — 3 commits for 1 fix signals no testing before push |
| `1338db5` | Daily DevOps report — critical 1.95 MB file size finding | ✅ YES — important signal |
| `caf117d` | PM report 2026-04-01 (v19) | Filed |

**Opportunity cost:** All April 1–2 code commits are minor flight URL patches. TP_MARKER, venue.facing, email capture backend, React pin — all flagged since v17 or earlier — remain unshipped on Day 10.

---

## Bug Triage — April 2

| Bug | Severity | Status | Days Open |
|-----|----------|--------|-----------|
| **TP_MARKER = "YOUR_TP_MARKER"** | **P0** | ❌ STILL UNSET | **Day 10+** |
| **app.jsx = 1.95 MB (Babel parse 5-10s on mobile)** | **P1 NEW** | ❌ DevOps flagged today | Day 1 |
| **venue.facing ?? 270 swell scoring bug** | **P1** | ❌ Still in code (`app.jsx:4683`) | Day 7 |
| **Email capture — no backend list** | **P1** | ❌ localStorage only | Day 10+ |
| **React CDN unpinned (`@18`)** | **P2** | ❌ Still unfixed | Day 7 |
| **Flight URL fragility** | **P2** | ⚠️ 4 patches on fragile code | Ongoing |
| **Photo duplication** | **P2** | ❌ Worse at 3,726 venues | Day 14+ |
| **Open-Meteo 429 handler missing in fetchWeather()** | **P2** | ❌ `app.jsx:4541` throws on 429 | Day 7 |

**New P1 — app.jsx is 1.95 MB.** DevOps measured it today: 10,961 lines, ~1.4 MB of which is VENUES data. Babel Standalone parses this at runtime on every cold load. On a mid-range Android at 3G — the typical Reddit mobile visitor — that is a 5-10 second blank screen before React renders anything. This is the highest-impact performance issue in the codebase and it is directly caused by expanding to 3,726 venues without extracting the data file.

---

## Known Blockers

| Blocker | Owner | Urgency |
|---------|-------|---------|
| **TP_MARKER placeholder** | Jack — tp.media → Markers → copy ID → `app.jsx:5316` → push | **P0. Day 10. Do it now.** |
| **app.jsx 1.95 MB** | Dev — extract VENUES to `venues.json`, load async | P1. 5-10s mobile load = bounce. |
| **Email capture no backend** | Dev — Loops.so POST on onboarding complete, 30 min | P1. Reddit spike leaves no list. |
| **venue.facing swell bug** | Dev — delete 5 lines in `scoreVenue()` (`app.jsx:4683–4691`) | P1. Wrong surf scores for 50% of breaks. |
| **LLC approval** | External | Unblocks REI (+$6.16 RPM), Backcountry, GetYourGuide, Stripe |
| **VPS /api/flights/latest confirmed live?** | Jack — `curl https://peakly-api.duckdns.org/api/flights/latest?origin=JFK&destination=BCN` | Unknown since March 29 |

---

## This Week's Top 3 Priorities

### 1. JACK: TP_MARKER. Day 10. Every click since March 24 = $0.

tp.media dashboard → Markers → copy your marker ID → replace `"YOUR_TP_MARKER"` at `app.jsx:5316` → push. Five minutes. This is revenue, not polish. It has appeared in every PM report for 10 consecutive days. If Reddit brought 300 visitors and 8% clicked a flight link, that's 24 unattributed, unmonetized conversions. With marker set, that's $15-40 at Reddit-traffic scale. Fix it today.

**Decision: SHIP. Jack. Today. Not tomorrow.**

### 2. DEV: Extract VENUES to venues.json — 3-4 hrs, P1 performance

app.jsx is 1.95 MB. ~1.4 MB is venue data. Babel parses all of it at runtime before React renders anything.

Fix: Move the VENUES array to `venues.json`. In app.jsx, on component init, `fetch('./venues.json').then(r=>r.json()).then(data => setVenues(data))`. Show a shimmer/loading state while venues load. This is not a split of the React app — it's data extraction. app.jsx stays single-file. First-paint time drops by ~50%.

This is the single highest-ROI engineering task available. It requires no design decisions, no architecture changes, and delivers measurable conversion improvement on mobile.

**Decision: SHIP this week. No new venues until this ships.**

### 3. DEV: Ship 4 unblocked sub-10-minute fixes (30 min total)

All flagged for 7+ days. Each under 10 minutes. All currently unshipped:

**a) venue.facing delete (10 min):** `app.jsx:4683–4691` — delete the `spotFacing`/`swellAlignment`/`swellAngleDiff` block. East-facing surf breaks get wrong scores. Any serious surfer on Reddit will notice.

**b) fetchWeather() 429 handler (5 min):** `app.jsx:4541` — add `if (r.status === 429) return null;` before the throw. fetchMarine() already handles this correctly. fetchWeather() crashes the Explore tab on rate limit.

**c) React pin (2 min):** `index.html:80–81` — change `react@18` → `react@18.3.1` and `react-dom@18` → `react-dom@18.3.1`. One CDN breaking change = all users' app is dead.

**d) Email capture POST to Loops.so (30 min):** On onboarding complete, `fetch()` POST to Loops.so free-tier webhook. Loops.so handles 2,500 contacts free. No LLC required. Without this, every email typed into the onboarding form since launch is lost.

**Decision: SHIP all 4. No design decisions required. Pure risk reduction.**

---

## Explicit Product Decisions This Report

**DECISION 1: VENUES freeze — no expansion until venues.json extraction ships.**  
File is 1.95 MB. Any new venue = worse mobile load time. Freeze. The 3,726 we have is more than enough to demonstrate value.

**DECISION 2: Flight URL code — stop patching, plan a clean rewrite.**  
4 commits in 7 days on buildFlightUrl(). The next touch on this function should be a clean rewrite with explicit parameter validation, not another try/catch layer. Not urgent — but document it for the next dev session.

**DECISION 3: App performance is now the #1 product risk, not features.**  
Every feature discussion this week is premature while users are bouncing at a 5-10 second blank screen. venues.json extraction ships before any new feature work.

**DECISION 4: Trips + Wishlists tabs — DEFER to 1K users.**  
CLAUDE.md says "ACTION NEEDED: Expose Trips and Wishlists." That was wrong when written. It is still wrong. 3-tab nav is correct for zero-to-first-1K. Core flow converts first.

**DECISION 5: Strike alerts server polling — DEFER to 100 alert subscribers.**  
Push infrastructure is live. Server polling before there are subscribers to push to is premature. Validate demand first.

---

## Features REJECTED This Week

| Feature | Decision | Reason |
|---------|----------|--------|
| More venue expansion | **CUT until venues.json ships** | File is 1.95 MB. Adding venues makes load time worse. |
| Trips + Wishlists tab expose | **DEFER to 1K users** | Core flow first. Standing decision. |
| Strike alerts server polling | **DEFER to 100 subscribers** | Build the list before building the push. |
| iOS App Store (Capacitor) | **DEFER to 500 validated users** | $99 + 4-week review. Validate PMF first. |
| Google Play via PWABuilder | **DEFER to week 3 post-Reddit** | $25, no code. Amplify existing users, don't create them. |
| Amazon links for 5 zero-affiliate categories | **SHIP week 2** | Correct, low-effort. Bundle with next code drop. Not this week. |
| Dark mode | **CUT permanently** | No user signal. Not in 6 months. |

---

## Success Criteria & 90-Day Projection

**What defines success:**
- 100 users in first 48 hours post-Reddit = minimum viable signal
- Alert set-rate >5% of sessions = core value prop lands
- Flight click-rate >8% of sessions = monetization funnel working
- 30-day retention >20% = habit-forming

**5K vs 8K at 90 days:**

| Lever | 5K scenario | 8K scenario |
|-------|-------------|-------------|
| Mobile load time | 5-10s blank screen (1.95 MB parse) | <2s first paint (venues.json async) |
| TP_MARKER | Fixed after Reddit spike | Fixed before Reddit. First commissions tracked from day 1. |
| Email list | localStorage only — no list | 100+ emails → re-engagement when next condition window opens |
| Scoring accuracy | venue.facing bug wrong for ~50% of surf breaks | Fixed. r/surfing users trust the scores. |
| Retention mechanism | Users check once, churn when conditions don't match | Alert fires when target venue hits score → users return |

**Verdict:** The path to 8K requires loading fast → earning trust on first session → re-engaging when conditions change. None of these require new features. All require fixing what's already broken.

---

## The Product Risk Nobody Is Talking About

**A 1.95 MB Babel parse is the silent conversion killer that no report has addressed.**

Every growth, content, and UX report this week discusses features, venues, and copy. None mention that mobile users hit a 5-10 second blank screen before any React renders. DevOps measured this today. The Babel Standalone architecture was the right call at 200 venues. At 3,726 venues and 10,961 lines, it is actively harming conversion.

The fix (extract VENUES to venues.json) is 3-4 hours of work. The impact is measurable: halved first-paint time, lower bounce rate on mobile, no architecture change required. Every day this doesn't ship, mobile visitors (which is most Reddit traffic) are experiencing a blank screen that Plausible records as a bounce.

If Reddit brought 300 visitors and 60% bounced during the 5-10 second load window, we didn't get 300 users. We got 120. Fix the load time before the next traffic event.

---

*PM agent v21 — 2026-04-02*  
*Filed for: Jack (jjciluzzi@gmail.com)*
