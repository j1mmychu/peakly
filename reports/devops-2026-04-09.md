# DevOps Report — 2026-04-09

**System Status: UNABLE TO VERIFY (sandbox network blocked)**

---

## Uptime & Health Checks

Network egress is blocked from this Cowork sandbox environment. Could not reach any external endpoints via curl or WebFetch. **Manual verification required.**

Endpoints to check:

| Endpoint | Expected |
|----------|----------|
| https://j1mmychu.github.io/peakly/ | 200, page loads |
| https://peakly-api.duckdns.org/api/flights/latest?origin=JFK&destination=CUN | 200, JSON flight data |
| https://api.open-meteo.com/v1/forecast?latitude=40.71&longitude=-74.01&daily=temperature_2m_max | 200, JSON forecast |

**Jack action:** Spot-check these three URLs in a browser. Takes 30 seconds.

---

## HTTPS Status: DONE

Caddy + Let's Encrypt on `peakly-api.duckdns.org` has been live since 2026-03-25. Frontend uses HTTPS proxy URL. No mixed content issues.

---

## Performance Analysis

| Metric | Value | Assessment |
|--------|-------|------------|
| app.jsx lines | 10,993 | Large single file — still manageable |
| app.jsx size (raw) | ~1.95 MB | **WARNING: Nearly 2 MB of raw JSX** |
| Babel transpile overhead | ~2–3s on cold load (estimated) | Users on slow connections will feel this |
| Venue count | ~3,726 | All in-memory, large DOM if not paginated |
| Unsplash photo refs | 3,741 | URLs embedded inline |
| Unique photo IDs | 261 | **CRITICAL: 93% photo duplication** |

**Top duplicated photo (204 uses):** `photo-1529961482160-d7916734da85`
**Second most duplicated (202 uses):** `photo-1523819088009-c3ecf1e34000`

The file has grown ~500 lines since the CLAUDE.md estimate of ~10,500. At nearly 2 MB raw, first-load performance is a concern — Babel must parse and transpile the entire file before React can mount. This is not yet a crisis on fast connections but will hurt mobile users on 3G/4G.

**Pagination is live** (30 venues/page) which prevents DOM explosion. Good.

---

## Security Review

**Status: CLEAN (with one known issue)**

| Check | Result |
|-------|--------|
| API tokens in app.jsx | CLEAN — no secrets found |
| Travelpayouts token | Server-side only (confirmed by comment at line 5124) |
| Push notification tokens | Stored in localStorage only (peakly_push_token) — acceptable |
| Recent commits | No secrets in last 10 commits |

**Known issue (unchanged):** `TP_MARKER` at line 5316 still reads `"YOUR_TP_MARKER"`. This means **every flight click earns $0 in affiliate revenue.** This has been flagged since at least April 6. It's a 5-minute fix for Jack — just replace with the real marker from tp.media dashboard.

---

## Cache Buster Status

| Asset | Current Version | Days Stale |
|-------|----------------|------------|
| index.html cache buster | `v=20260331a` | **9 days stale** |
| Service worker CACHE_NAME | `peakly-20260330` | **10 days stale** |

**P2 but growing.** Last product commit was `fd6e4e8` on April 4 (5 days ago). Cache busters should be bumped on next deploy. If any code changes ship, both values need updating or users will serve stale content.

---

## Code Freeze Status

**Last product commit:** `fd6e4e8` — April 4, 2026 (5 days ago)
**Last any commit:** `ac46dae` — merge main into master (no product changes)

This is the **6th consecutive day with zero code shipped.** ProductHunt is 6 days out (April 15). All 7 P1 bugs from the April 6 audit remain open:

1. Venue duplicates (242 dupes, dangerous mis-tags)
2. Photo duplication (93% reuse, 261 unique out of 3,726)
3. Airport infrastructure gaps (expansion venues missing from lookups)
4. fetchWeather() no 429 handling
5. venue.facing swell bug (default 270)
6. Email capture has no backend
7. Score vote buttons still rendered

---

## Decision Made

**Cache buster bump is blocked on code changes.** No point bumping the cache buster if there's nothing new to serve. The real blocker is the code freeze — someone needs to start shipping P1 fixes. I'm flagging this as a process issue, not an infrastructure issue.

**Recommendation:** When the next code change ships, bump both `v=` in index.html and `CACHE_NAME` in sw.js simultaneously. Suggested values: `v=20260409a` and `peakly-20260409`.

---

## Cost Estimate

| Resource | Monthly Cost |
|----------|-------------|
| GitHub Pages | $0 (free) |
| DigitalOcean VPS (1GB) | ~$6/mo |
| DuckDNS domain | $0 (free) |
| Caddy + Let's Encrypt | $0 (free) |
| Plausible analytics | ~$9/mo (or self-hosted free) |
| Open-Meteo API | $0 (free tier) |
| Sentry | $0 (free tier) |
| UptimeRobot | $0 (free tier) |
| **Total** | **~$15/mo** |

---

## Summary

Infrastructure is stable as configured. The real risk right now is not reliability — it's velocity. Six days of zero commits with ProductHunt in 6 days. The P1 bugs (especially venue duplicates with dangerous mis-tags like Teahupo'o labeled "All Levels") are a reputational risk, not just a polish issue.

**Top 3 actions for Jack:**
1. Replace `YOUR_TP_MARKER` with real marker (5 min, unlocks flight revenue)
2. Start shipping P1 fixes — any one of the 7 open bugs
3. Spot-check the three health endpoints listed above (30 sec)
