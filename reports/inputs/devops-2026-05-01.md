# DevOps Report — 2026-05-01

**System Status: DEGRADED — uncommitted launch-blocker fixes sitting in working tree for 16 days. Live endpoints unverifiable from sandbox.**

---

## 1. Uptime & Health Checks

> ⚠️ Sandbox network egress is restricted (allowlist excludes `j1mmychu.github.io`, `peakly-api.duckdns.org`, `api.open-meteo.com`). Live uptime checks could not be executed today — same constraint as the 04-16 report. All findings below are code/config based.

| Endpoint | Status | Notes |
|----------|--------|-------|
| GitHub Pages (`j1mmychu.github.io/peakly/`) | UNVERIFIED | Sandbox-blocked. Manual: `curl -I https://j1mmychu.github.io/peakly/` |
| VPS proxy (`peakly-api.duckdns.org/health`) | UNVERIFIED | Sandbox-blocked. Manual: `curl https://peakly-api.duckdns.org/health` |
| Open-Meteo weather API | UNVERIFIED | Sandbox-blocked |

**Recommendation:** Stand up free UptimeRobot monitors on both URLs — eliminates the visibility gap entirely. 5-min intervals, free tier, email alert on downtime.

> Note: the agent task file (`tasks/agents/devops.md`) is stale. It still references the old HTTP-only proxy at `104.131.82.242:3001`. Reality (per CLAUDE.md): VPS is `198.199.80.21`, HTTPS-fronted at `peakly-api.duckdns.org` via Caddy. Fix the task file so future runs don't waste cycles checking a dead address.

---

## 2. HTTPS Migration Status

**DONE.** Caddy + Let's Encrypt terminate TLS on `peakly-api.duckdns.org` and reverse-proxy to `127.0.0.1:3001`. Proxy is not directly internet-exposed. Frontend `FLIGHT_PROXY` constant in `app.jsx:1399` correctly points at the HTTPS hostname. No mixed-content risk. CORS allowlist (`proxy.js:50-58`) is HTTPS-only for production origins.

---

## 3. Performance Analysis

| Metric | Value | Notes |
|--------|-------|-------|
| `app.jsx` raw size | 463 KB (474,409 B) | Single-file by design |
| `app.jsx` line count | 7,137 | Unchanged since 04-16 |
| Hooks call sites | 107 | Up from 97 — new wiring landing in feature branches |
| Babel Standalone (CDN) | ~600 KB | Cached after first load; biggest cold-start cost |
| Service worker cache | `peakly-20260414b` | 17 days old. Acceptable — no shipped client change since Apr 15 |

Bottlenecks unchanged from 04-16: Babel runtime transpile is the cold-start tax (deliberate trade-off, no build step), weather batches are intentional (50/2s), images use `loading="lazy"`, no list virtualization yet (fine at 231 venues).

---

## 4. Security Review

**Client code: CLEAN.** Grep for tokens / API keys / Bearer / `AIza*` / Stripe keys across `app.jsx`, `index.html`, `sw.js`, `manifest.json` — zero hits. `TRAVELPAYOUTS_TOKEN` is server-env-only. `TP_MARKER=710303` is the public affiliate ID. Sentry CDN key is a public DSN.

**Proxy hardening confirmed:**
- 60 req/min/IP rate limiter with 5-min stale cleanup (`proxy.js:24-48`)
- IATA regex validation on flight endpoints (`proxy.js:101,110,171`)
- 16 KB JSON body cap (`proxy.js:9`)
- CORS allowlist scoped to known origins
- Alert capacity cap with 503 response (`proxy.js:254`)

**Open issues (carryover, not new):**
- No SRI on unpkg CDN scripts (react/react-dom/babel) — medium risk, post-launch
- No CSP meta tag — low/medium risk on static GH Pages site, post-launch

---

## 5. 🚨 Repo State — the real issue today

```
M  CLAUDE.md
M  server/proxy.js                  (-23 lines: duplicate /api/waitlist handler removed)
?? reports/devops-2026-04-16.md
?? reports/pm-2026-04-15.md
?? reports/revenue-2026-04-15.md
?? reports/revenue-2026-04-16.md
?? reports/ux-2026-04-13.md ... ux-2026-04-16.md
```

**Last commit to `main` was 2026-04-15 — 16 days ago.** The duplicate-waitlist-handler fix the 04-16 report flagged got applied locally but was never committed or pushed. **Production proxy.js still has the bug** (and ignores the second handler with proper email regex + IP/source attribution). Eight agent reports are also uncommitted.

This is the highest-priority infra item right now. Forget Cloudflare, forget CSP — the fix is already written and just needs to ship.

---

## 6. Decision Made

**Ship the pending commits today and redeploy the proxy.** One copy-paste:

```bash
cd ~/peakly && \
git add CLAUDE.md server/proxy.js reports/ && \
git commit -m "ship: waitlist handler dedupe + 16d of agent reports" && \
git push origin main && \
ssh root@198.199.80.21 'cd /opt/peakly-proxy && git pull && systemctl restart peakly-proxy'
```

After deploy, `curl https://peakly-api.duckdns.org/health` from your laptop to confirm.

---

## 7. Self-improvement nudges

- **Add UptimeRobot** (free) for both URLs. Closes the unverifiable-from-sandbox gap permanently. 5 minutes of setup.
- **Refresh `tasks/agents/devops.md`** so it points at the HTTPS proxy on `198.199.80.21` instead of the old `104.131.82.242:3001`. Stale task files quietly degrade every future agent run.
- **Schedule a `git push` reminder** — 16 days of local-only fixes is a deploy hazard. Either daily auto-commit of reports, or a cron nudge if `git status` is dirty for >24h.

---

## 8. Infrastructure Cost

| Item | Cost |
|------|------|
| DigitalOcean VPS (1 GB, Ubuntu 24) | ~$6/mo |
| GitHub Pages, Open-Meteo, DuckDNS, Caddy/LE, Travelpayouts | Free |
| **Total** | **~$6/month** |

UptimeRobot recommendation above stays free.

---

*DevOps agent — 2026-05-01*
