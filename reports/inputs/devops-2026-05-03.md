# DevOps Report — 2026-05-03

**System Status: DEGRADED — 18 days of uncommitted fixes in working tree, including a real proxy bug. Live endpoints unverifiable from sandbox.**

---

## 1. Uptime & Health Checks

> ⚠️ Sandbox network egress is restricted (allowlist excludes `j1mmychu.github.io`, `peakly-api.duckdns.org`, `api.open-meteo.com`). Live uptime checks could not be executed today — same constraint as 04-16 and 05-01. All findings below are code/config based.

| Endpoint | Status | Manual verification |
|----------|--------|---------------------|
| GitHub Pages (`j1mmychu.github.io/peakly/`) | UNVERIFIED | `curl -I https://j1mmychu.github.io/peakly/` |
| VPS proxy (`peakly-api.duckdns.org/health`) | UNVERIFIED | `curl https://peakly-api.duckdns.org/health` |
| Open-Meteo weather API | UNVERIFIED | `curl 'https://api.open-meteo.com/v1/forecast?latitude=40.71&longitude=-74.01&daily=temperature_2m_max'` |

**Recommendation (3rd time):** Stand up free UptimeRobot monitors on both URLs. 5-min interval, free tier, email alert on downtime. Closes the visibility gap permanently.

> Stale task file: `tasks/agents/devops.md` still points at the old HTTP-only address `104.131.82.242:3001`. Reality (per CLAUDE.md): VPS is `198.199.80.21`, HTTPS-fronted at `peakly-api.duckdns.org` via Caddy. Flagged on 05-01, still not patched.

---

## 2. HTTPS Migration Status

**DONE.** Caddy + Let's Encrypt terminate TLS on `peakly-api.duckdns.org` and reverse-proxy to `127.0.0.1:3001`. Proxy is not directly internet-exposed. Frontend `FLIGHT_PROXY` in `app.jsx` correctly points at the HTTPS hostname. CORS allowlist (`proxy.js`) is HTTPS-only for production origins. No mixed-content risk.

---

## 3. Performance Analysis

| Metric | Value | Notes |
|--------|-------|-------|
| `app.jsx` raw size | 464 KB (474,409 B) | Unchanged since 04-15 |
| `app.jsx` line count | 7,137 | Unchanged |
| `index.html` | 16 KB / 370 lines | |
| `sw.js` cache key | `peakly-20260414b` | 19 days old (acceptable — no shipped client change since 04-15) |
| Babel Standalone (CDN) | ~600 KB | Cached after first load; biggest cold-start cost |

Bottlenecks unchanged: Babel runtime transpile is the cold-start tax (deliberate trade-off, no build step). Weather batches (50/2s) are intentional. Images use `loading="lazy"`. No list virtualization needed at 231 venues.

---

## 4. Security Review

**Client code: CLEAN.** Grep across `app.jsx`, `index.html`, `sw.js`, `manifest.json` for `api[_-]?key | secret | token | bearer | AIza | sk_live | sk_test | pk_live` returns zero real hits — only false positives (the word "token" inside a `SyntaxError` message and a Capacitor PushNotifications listener parameter named `token`). `TRAVELPAYOUTS_TOKEN` is server-env-only. `TP_MARKER=710303` is the public affiliate ID. Sentry CDN key is a public DSN.

**Proxy hardening confirmed (working tree):**
- 60 req/min/IP rate limiter with 5-min stale cleanup (`proxy.js:24-48`)
- IATA regex validation on flight endpoints
- 16 KB JSON body cap
- CORS allowlist scoped to known origins
- Alert capacity cap with 503 response

**Open carryovers (not new, post-launch):**
- No SRI on unpkg CDN scripts (react, react-dom, babel) — medium risk
- No CSP meta — low/medium risk on a static GH Pages site

---

## 5. 🚨 Repo State — the actual problem today

**18 days since the last shipped commit.** Last commit: `a9a01e3` on 2026-04-15. Working tree has been sitting on uncommitted changes for nearly three weeks. Same finding as 05-01, now two days older.

```
M  CLAUDE.md       (state notes — current as of 2026-05-02)
M  server/proxy.js (real bug fix — see below)
?? reports/devops-2026-04-16.md
?? reports/devops-2026-05-01.md
?? reports/pm-2026-04-15.md
?? reports/revenue-2026-04-15.md
?? reports/revenue-2026-04-16.md
?? reports/ux-2026-04-13.md … 04-16.md
```

### The proxy.js change is not noise — it's a fix

**HEAD has a duplicate `/api/waitlist` endpoint.** Two registrations: lines 228 and 335 of `proxy.js` in HEAD. With Express, the first registration wins and the second is dead code, but it's confusing and the two implementations differ (path constants, mkdirSync placement, response payload). Working tree de-dupes to a single canonical handler at line 312. HEAD endpoint count: 7. Working tree: 6.

Whichever version is currently running on the VPS depends on which file was last deployed there — that should be verified against the working-tree version once shipped. Manual check:

```bash
ssh root@198.199.80.21 'grep -c "app.post.\x27/api/waitlist" /opt/peakly-proxy/proxy.js'
```

If that returns `2`, prod has the duplicate. If `1`, prod is already on a clean deploy that never made it back into git.

### Recommendation: ship today

```bash
cd ~/peakly
git add CLAUDE.md server/proxy.js reports/
git commit -m "chore: dedupe /api/waitlist + state notes + agent reports"
git push origin main
ssh root@198.199.80.21 'cd /opt/peakly-proxy && git pull && systemctl restart peakly-proxy'
```

---

## 6. Decision Made

**Push the working tree this session, then add UptimeRobot monitors before the next devops run.** The repo divergence is the real reliability risk right now — every day it sits, a future hotfix has to either rebase across local-only changes or stomp them. UptimeRobot setup is 5 minutes and finally closes the "uptime: UNVERIFIED" line that's been sitting in this report for three runs straight.

---

## 7. Cost

| Line item | Monthly |
|-----------|---------|
| DigitalOcean droplet (1 GB, Ubuntu 24) | ~$6 |
| DuckDNS dynamic DNS | $0 |
| GitHub Pages | $0 |
| Open-Meteo weather/marine | $0 |
| Caddy + Let's Encrypt TLS | $0 |
| Travelpayouts (revenue, not cost) | — |
| **Total infra** | **~$6/mo** |

Pending one-time additions before launch (per CLAUDE.md, not infra-blocking): Apple Developer enrollment ($99/yr), Google Play one-time ($25), `peakly.app` domain.
