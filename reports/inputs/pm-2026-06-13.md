# Peakly PM Report — 2026-06-13 (v56)

> Supersedes v55 (June 11). **Status: GREEN, but the green is now silence.** Zero code regressions, zero open code blockers — and **zero commits in 3.4 days** (last real commit `dcc65f8`/`c70824f`, June 10 19:25 PDT). Every v55 decision still stands; **none of the Jack-manual actions they gated have moved.** The product is done code-side and parked entirely on a manual queue that isn't advancing. The signal this run is the lack of one.
>
> _Run note: scheduled run from the Cowork sandbox. `raw.githubusercontent` 403 (sandbox egress allowlist, not GitHub) — canonical prompt read from the mounted repo. Direct live-site fetch also blocked from sandbox; code-state facts below are eval-verified against the working tree. Date: session clock says 2026-06-13; UTC is 2026-06-14 05:34 — dated 06-13 per the session's authoritative date._

---

## Shipped Since v55 (June 11)

**Nothing.** No commits since June 10 evening. This is the headline.

`git log --since="2026-06-10 19:30"` → empty. The repo has not changed in ~3.4 days. v55's June-11 work product (the PM report, the DevOps report, the regenerated eager-Supabase diff) was written but **never committed** — it's sitting untracked in the working tree right now (Open #11 manifesting live; see Risk). There is no June-12 report at all — the PM/DevOps slots either didn't fire or didn't persist.

**Code state (eval-verified this run, not grep):** 353 venues (130 ski / 223 beach), 0 dup IDs · braces 5509/5509 · `GEAR_ITEMS` 0 · cache `20260610af` lockstep across app.jsx/sw.js/index.html · app.jsx 13,021 lines · tree otherwise matches origin. Healthy and frozen.

---

## Bug Triage — June 13

| Item | Severity | Status |
|------|----------|--------|
| **3.4 days, no commits + June-11 reports still uncommitted** | **P1 (process)** | The auto-push hook only catches Jack's local Edits, not scheduled agents (Open #11). Three days of agent output never landed. One crontab line ends it. Decision 1. |
| **Account-deletion SQL not pasted into Supabase** | **P0 for App Store** (not web) | Unchanged since v55. `delete-account.sql` is committed; the Supabase paste is the 2-min Jack action. Until done, the Delete-account UI shows the "email us" fallback — itself a 5.1.1(v) rejection risk. Still the #1 submission gate. |
| Eager-Supabase `<script>` still live at index.html:85 (2.106.2) | P2 | Confirmed still present this run; v55 Decision 1 diff re-verified `git apply`-clean. Pure waiting-on-paste. Not re-litigated below (two-strikes — it's a decision + a ready diff, not a finding). |
| Daily-briefing routine still never scheduled — `reports/briefings/` empty | P2 | v55 Decision 5 unactioned. Not relitigating; folded into Decision 1. |

No new P0/P1 *code* bugs. No regressions introduced (nothing was introduced at all).

---

## Known Blockers (all Jack-manual; zero code blockers — unchanged from v55, none advanced)

| Blocker | Unlocks | Effort | Movement since v55 |
|---------|---------|--------|--------------------|
| **Supabase SQL paste** (`delete-account.sql`) | iOS submission (5.1.1(v)) | 2 min | None |
| Apple Developer enrollment ($99) + `cap add ios` + Xcode build | iOS App Store | ~1–2h + Apple queue | None |
| LLC approval | REI +$6.16, Backcountry/GYG +$1.84 | External | None |
| `peakly.app` domain + placeholder affiliate IDs | Brand + revenue | 30 min | None |

The entire table is identical to v55. That's the problem.

---

## Explicit Product Decisions — June 13

### Decision 1: Install the agent-commit cron line TODAY — the pipeline is silently dropping work.
Open #11 has now produced hard evidence: an entire day's agent output (June 11 PM + DevOps reports + the regenerated diff) never committed, and there's a missing June-12 cycle. Every report after an uncommitted one reads stale state and re-derives findings the prior run already closed — exactly the loop this whole agent system exists to avoid. CLAUDE.md #11 already specifies the fix: `45 17 * * * cd ~/peakly && bash scripts/auto-push.sh` in local crontab (after the 17:30 briefing slot). Two-minute install. **SHIP — this is the highest-leverage 2 minutes on the board because it's the one that makes every *other* agent action actually persist.**

### Decision 2: Re-affirm the v55 manual queue as-is — do NOT add new scope while the queue is stalled.
Five Jack-manual items have sat unmoved for 3+ days. The correct PM response to a stalled queue is not to add to it. No new features, no new venues (catalog is FROZEN per v55 Decision 3 and stays frozen), no new infra tasks. The job this week is to *drain* the existing five, in this order: (1) the two 2-min pastes [Supabase SQL + eager-Supabase diff], (2) start Apple enrollment so its queue clock runs in parallel. **HOLD all new scope until the queue clears.**

### Decision 3: Launch-window clock is now a first-class line item, not a footnote.
v55 noted "every week past mid-July halves the summer-beach runway." It's June 13. The beach half of the catalog (223 of 353 venues) is the demand driver for a June–August Reddit/HN push, and that window is the 8K-vs-5K differentiator. With the repo idle and Apple's review queue (days-to-weeks) not yet even started, the realistic earliest App Store live date is drifting toward the back half of the window. **CALL IT:** if Apple enrollment hasn't started by end of next week (~June 20), the iOS summer-beach launch should be explicitly re-planned as a web-PWA-first Reddit launch (which needs *zero* Apple dependencies and could go this week), with the App Store following whenever it clears. Don't let the iOS dependency hold the web launch hostage.

### Decision 4: App Review notes for magic-link auth — pre-write them now (carries over from v55's risk, promoted to a decision).
v55 flagged magic-link-only auth as the unspoken App Review risk. Promote it: before the Xcode build, the submission packet must state that sign-in is optional and the anonymous flow is the full product (true today), OR provide a reviewer-workable test path. Zero code. ~20 min of writing. **SHIP into the submission checklist now so it's not discovered at review time.**

---

## This Week's Top 3 Priorities Only

1. **Make the agents persist (Decision 1).** One crontab line. Without it, every report below — including this one — risks evaporating, and the team keeps re-litigating closed items. This is the meta-fix.
2. **Drain the two 2-minute pastes (Supabase SQL + eager-Supabase diff).** Combined ~5 min; one unblocks the App Store, the other speeds every anonymous first paint. Both have been ready since June 11.
3. **Decide the launch path (Decision 3): start Apple enrollment, OR commit to a web-PWA-first Reddit launch this week.** Either is fine; continued drift is not. The summer-beach window is the asset depreciating fastest.

---

## Features REJECTED This Week

| Feature | Decision | Reason |
|---------|----------|--------|
| Any new venue beyond the frozen catalog | **REJECTED — freeze holds** | Open-Meteo free-tier headroom is the binding constraint (v55 D3). Adding to a stalled queue is the wrong move (D2). |
| New infra/feature work of any kind | **HOLD** | Five manual items unmoved 3+ days; drain before adding. |
| SRI + CSP hardening | **DEFER July** | Unchanged. Babel `unsafe-eval` regression risk. |
| Hotels / Pro UI / Wishlists+Trips tabs / surfing reinstatement | **CUT / LOCKED — final** | Not relitigating. |

---

## Success Criteria — Reddit launch (48h post-post)

Thresholds unchanged from v55 (success ≥500 visits / ≥8% action rate / top-10 subreddit post / proxy errors <1% / no ErrorBoundary in Sentry; accelerate ≥1,500 visits; pause if bounce >75% or stale-fare reports). One addition tied to Decision 3: **the web-PWA launch has no Apple dependency and could run this week** — if the iOS path keeps slipping, the success criteria should be measured against a web-first launch, not held until App Store approval.

---

## One Product Risk Nobody Is Talking About

**The system is now optimized to produce reports about a product that isn't shipping.** Five agents run daily and generate well-reasoned analysis, but the underlying artifact (the repo) hasn't changed in 3.4 days and the actions that *would* change the product are all outside the agents' reach (Jack-manual) — and several aren't even persisting (Open #11). The risk is a self-sustaining illusion of progress: green reports, rising version numbers (v54→v55→v56), zero forward motion on the actual launch. The honest read is that Peakly has been **launch-ready since June 10** and the only variable left is whether Jack pulls the trigger. The agent cadence should arguably *throttle down* (Decision 5 territory) to a weekly check until a launch date exists — daily reports on a frozen repo are noise that buries the one real signal: nothing shipped.

---

## Diffs ready to apply (carryover — both still unapplied)

- `reports/ready-to-ship/eager-supabase-delete-2026-06-11.diff` — re-verified `git apply`-clean this run. Delete the corrupt 05-08 predecessor when it lands.

---

*Written by the scheduled PM agent run, 2026-06-13. Dated copy at `reports/inputs/pm-2026-06-13.md`; rolling copy at `reports/pm-report.md`. ⚠️ Both — plus the still-untracked June-11 reports — remain uncommitted until the auto-push hook or a manual commit catches them (Open #11). `push "pm + devops June 11–13 catchup"` clears the backlog. Jack handles git.*
