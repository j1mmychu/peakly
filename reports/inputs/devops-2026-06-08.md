# Peakly DevOps Report — 2026-06-08

**Status: 🟢 GREEN**

Quietest run in weeks. Security is clean, repo is in sync (working tree clean on `master`), cache stamps are consistent across all three files. The big news: **the cache-buster auto-bump fix that DevOps recommended on 05-26 / 05-31 / 06-04 is now live in `scripts/auto-push.sh`** — the recurring stale-buster P0 is structurally dead. The two chronic P1s (VPS redeploy, APNS) have now appeared unchanged in three consecutive reports and graduate to `known-skipped.md` this run per the two-strikes rule; both are Jack-only manual actions and neither blocks the live web product. One new P2: a stale prunable git worktree on disk.

> **Scope note (honest):** Live HTTP probing of the GitHub Pages site, the VPS `/health` endpoint, and Open-Meteo was **not available this run** — `web_fetch` is provenance-gated to user-supplied URLs and the scheduled sandbox blocks ad-hoc `curl`. Every check below is a **static** verification against the repo. Response-time / uptime rows are intentionally omitted rather than fabricated. If live synthetic monitoring matters (it does pre-launch), the right fix is an external monitor — see the standing UptimeRobot item in `known-skipped.md`.

---

## Fixes Shipped This Run

None required. No cache bump needed — working tree is clean, so the deployed stamp (`20260607ae`) correctly matches deployed code. (Auto-push now bumps automatically the moment any of `app.jsx` / `sw.js` / `index.html` change; see §1.)

---

## 1. Live Site Health (static)

| Check | Result |
|-------|--------|
| `app.jsx` size | **9,002 lines / 533,592 bytes** (~521 KB raw, ~150 KB gzip est.) |
| Git working tree | **Clean** on `master`, in sync with `origin/main` ✅ |
| Cache buster (`index.html:400`) | `v=20260607ae` ✅ matches code |
| SW CACHE_NAME (`sw.js:2`) | `peakly-20260607ae` ✅ locked in step |
| PEAKLY_BUILD (`app.jsx:17`) | `20260607ae` ✅ locked in step |
| CDN version pins | React/ReactDOM `18.3.1`, Supabase `2.106.2`, Leaflet `1.9.4` — all pinned ✅ (last live-version verification: 06-04 report) |
| Plausible analytics | Present at `index.html:32`, uncommented ✅ |
| Sentry DSN | Active, non-empty (`...@o4511108649058304.ingest.us.sentry.io`) ✅ |

### ✅ RESOLVED — Cache-buster auto-bump is now implemented

`scripts/auto-push.sh` (lines 40–86) now self-bumps the build stamp whenever a cache-relevant file changes: reads `PEAKLY_BUILD`, resets to `${TODAY}a` if the stamp is from a prior day, otherwise increments the suffix (`a→b→…→z→aa`), and rewrites all three files (`app.jsx`, `sw.js`, `index.html`) in lockstep via `perl -pi`. This is exactly the fix DevOps proposed three runs running. The recurring "stale buster" P0 class is now structurally eliminated. **Stop reporting it.**

> Side note: the current suffix `…ae` reflects ~31 auto-pushes on 06-07 (the hook fires per Edit/Write). That's expected noise, not a bug. The stamp only matters for cache correctness, and it's correct.

---

## 2. Flight Proxy / APNS (static — not live-probed this run)

Code state verified from `server/proxy.js`:

- Secrets are read exclusively from `process.env` — `TRAVELPAYOUTS_TOKEN` (line 12), `APNS_KEY_ID` / `APNS_TEAM_ID` / `APNS_BUNDLE_ID` / `APNS_KEY_PATH` / `APNS_PROD` (lines 492–549). **No hardcoded token literals.** ✅
- Weekend-specific date support + `/api/weather` + `/api/marine` shared-cache endpoints are present and committed.

The redeploy-to-VPS and APNS-key items are **not new findings** — see §6 (graduated to known-skipped). Live `/health` reachability and response time could not be measured this run; if Jack wants a live read, `curl https://peakly-api.duckdns.org/health` from his shell answers it in one line.

---

## 3. Security Audit — 🟢 CLEAN

| Check | Result |
|-------|--------|
| Secret scan (`ghp_`, `github_pat_`, `sk_live`, AWS keys, private-key headers) across `*.jsx/js/html/json/md/sh` | **Zero hits** ✅ |
| Travelpayouts token on client | Not present. Only public `TP_MARKER = "710303"` (`app.jsx:1904`) — affiliate marker, embedded in deep links by design ✅ |
| `.env` patterns in `.gitignore` | `.env`, `.env.*`, `.env.local`, `*.env` all covered ✅ |
| Secrets tracked in git | None (`git ls-files` shows no `.env` / `.p8` / `.pem`) ✅ |
| Proxy secret handling | All via `process.env`, no literals ✅ |
| Supabase client key | Anon key only (public-safe, RLS-gated) ✅ |
| SRI on CDN scripts | 2 of ~5 — known/decided, see `known-skipped.md` (2026-05-04) |
| CSP meta tag | Absent — known/decided, see `known-skipped.md` (2026-05-04) |

No action required.

---

## 4. Performance — static

- `app.jsx` is ~521 KB raw / ~150 KB gzip est. Largest single payload; unchanged shape since 06-04 (+6 lines). Still well within tolerance for a single-file SPA.
- **Largest unaddressed perf lever:** Unsplash venue images — 160 URLs at `w=800` with no `auto=format` / `fm=webp` / `q=` param. This is the standing `known-skipped` item (re-flags at MAU > 100 or LCP regression). Not re-filed.
- Eager Supabase `<script>` (~80 KB gzip to anon visitors) — `known-skipped`, Rule-1 diff already staged at `reports/ready-to-ship/eager-supabase-delete-2026-05-08.diff`.

### P2-A (NEW) — Stale prunable git worktree on disk

`git worktree list` shows a `prunable` worktree at `.claude/worktrees/quirky-darwin` (3.9 MB, holding a ~10,600-line stale copy of `app.jsx` with placeholder `TP_MARKER="YOUR_TP_MARKER"`). It's `.gitignore`'d (0 tracked files, no git or security impact) but it sits on disk, pollutes repo-wide greps, and is a leftover from the worktree-heavy cloud-agent era cleaned up on 05-09. One-command fix:

```bash
cd ~/peakly && git worktree prune && rm -rf .claude/worktrees/quirky-darwin
```

Est. time: 10 seconds. Risk: low (gitignored, prunable, placeholder content only).

---

## 5. Cost Estimate

Unchanged from 06-04. DigitalOcean droplet $6/mo. All external APIs (Open-Meteo, Plausible, Sentry, Supabase) on free tiers, nowhere near limits at current <10 MAU. Projection: 1K MAU ≈ $6/mo (droplet only); 10K MAU ≈ $6–12/mo (watch Open-Meteo request volume — the proxy weather cache, once deployed, collapses N client calls to ~229 cached coords); 100K MAU ≈ upgrade to $12 droplet (2 GB) before the cache LRU thrashes. No new cost pressure today.

---

## 6. Two-Strikes Graduations (chronic P1s → known-skipped)

Both items below appeared unchanged in **05-31 and 06-04** (the last two reports). Per the two-strikes rule, neither is a Rule-1 code diff (both require Jack's manual SSH / Apple-console access), and neither blocks the live web product. They are now implicit decisions. **Moved to `reports/known-skipped.md` this run; DevOps stops re-surfacing them.**

| Item | Status | Why it's not a DevOps finding anymore |
|------|--------|----------------------------------------|
| VPS proxy redeploy (~Day 35) | Code committed, never deployed | Site runs fine on direct Open-Meteo + legacy pricing. Redeploy is an enhancement. Re-flags at MAU > 100 or a wrong-fare user report. |
| APNS keys (~23d past 05-13 deadline) | `apns_configured: false` | Web product unaffected. CLAUDE.md #12 fallback (gate Alerts tab behind `isNativePlatform()`) ships iOS v1 without push. Re-flags only if an App Store submission is queued. |

---

## 7. What Breaks First at Scale

Same answer as the last several runs, and it's no longer the cache buster (that's fixed): **Open-Meteo's free-tier rate ceiling.** Every client fetches weather directly because the proxy `/api/weather` cache is coded but undeployed. At roughly 66+ concurrent DAU hitting the same venue set, requests start getting throttled and venue cards silently degrade to error/empty states with no user-facing explanation. The prevention is already written — it's the VPS redeploy in §6. The instant a Reddit/HN post lands, that redeploy jumps from "enhancement" back to P0; the known-skipped entry says exactly that. Until traffic exists, it correctly stays parked.

---

## Diffs Ready to Apply

None new this run. Standing staged diffs (unchanged, Jack pastes when ready): `eager-supabase-delete-2026-05-08.diff`. The P2-A worktree prune above is a shell command, not a code diff, so it's inline rather than in `ready-to-ship/`.

## Action Items

| # | Item | Time | Owner |
|---|------|------|-------|
| 1 | `git worktree prune && rm -rf .claude/worktrees/quirky-darwin` | 10 sec | Jack (optional housekeeping) |
| 2 | (Parked, not nagging) VPS redeploy + APNS — see known-skipped | — | Jack |
