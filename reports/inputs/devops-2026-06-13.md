# Peakly DevOps Report — 2026-06-13

**Status: 🟢 GREEN** (one 🟡 repo-hygiene snag, Jack-only, zero launch/runtime impact)

Live site healthy, all three VPS endpoints serving, security clean, every code invariant holds (braces 5509/5509, venues 353, GEAR_ITEMS 0 — correct per Jack's v1 cut, cache stamp lockstep). Zero P0s, zero P1s on the product. The one new item is a local↔origin reports-tree divergence with a latent merge conflict on `reports/pm-report.md` — it touches no code and can't break the live site, but it'll bite the next `git pull`/auto-push. Details in §7.

> Run note: executed from the Cowork sandbox. Sandbox egress blocks outbound curl (403 at the proxy), so live checks ran through the **connected Chrome browser** (Browser 1, macOS) — real HTTP, real render. Response-time numbers aren't available from that path, so latency is reported qualitatively, not guessed. Canonical `tasks/agents/devops.md` was read from the mounted working tree (raw.githubusercontent 403s from the sandbox — same allowlist limit, not a GitHub outage); the tree is the file the raw URL serves.

---

## 1. Live Site Health — ✅

| Check | Result |
|-------|--------|
| https://j1mmychu.github.io/peakly/ | HTTP 200, full render via Chrome |
| Hero / Best Window | "Banje Beach Dubrovnik 96/100 GO" (Dubrovnik, UV 7.7 · 79.9°F · Full sun, ~$750 from LAX) |
| Weekend window | "Sat, Jun 13 → Mon, Jun 15" — date math correct for today |
| Grid | 30-card grid + "Show more (192 remaining)" = **222 beach spots**; real weather (UV/temp/condition) + ~$ fares populated; later cards show the expected "Checking conditions…" in-flight state |
| Per-day scoring | "Best: Tomorrow · 100/100", "Best: Mon Jun 15 · 94/100", etc. rendering correctly |
| Console errors | None (caveat: tracking attaches post-load; no ErrorBoundary fallback in the DOM, every card rendered) |
| Title/SEO | "Peakly — Best Ski & Beach Weekend Trips with Cheap Flights" ✅ |
| Cache stamp | `20260610af` — **lockstep across app.jsx / sw.js / index.html ✅.** From 06-10 because no edits since; auto-bump fires on edit, so this is expected, not stale (stale-stamp class is structurally dead per CLAUDE.md). |

## 2. Flight Proxy Health — ✅ (all three endpoints serving)

| Endpoint | Result |
|----------|--------|
| `/health` | `{"status":"ok"}`, uptime **274,675s (~3.18 days)** — consistent with the 06-10 evening kernel reboot, pm2 resurrection holding; `wx_cache_size: 493` (warm, up from 370 on 06-11), `wx_inflight: 0`, poll worker last ran `2026-06-14T05:17Z` with **0 errors / 0 alerts**, `apns: "unconfigured"` (parked, known-skipped) |
| `/api/weather?lat=50.12&lon=-122.96` | `success:true`, full 7-day Open-Meteo daily structure (Whistler, dates `2026-06-13`→`06-19` correct), `cached:false` on first hit — cache write path working |
| `/api/flights?origin=LAX&destination=JFK` | `success:true`, valid structure (`{"JFK":{"2026-06":{"price":181,"depart_date":"2026-06-29"}}}`), fresh `found_at` timestamp |

## 3. Weather & External APIs — ✅

Open-Meteo verified healthy via the proxy round-trip (full 7-day structure upstream, generationtime ~1.5ms). Free-tier headroom unchanged from 06-11: 353 venues × ~12 cache refills/day + marine for 223 beach ≈ 5–7K upstream calls/day worst case from the single VPS IP — under the ~10K/day non-commercial ceiling, but post venue-doubling no longer trivially so. See §8.

## 4. Security Audit — 🟢 CLEAN

| Check | Result |
|-------|--------|
| Secret scan (`ghp_`, `github_pat_`, `sk-`, `xox*`, AWS `AKIA`, private-key headers) across js/jsx/html/json/sh/md, ex-node_modules | **Zero hits** ✅ |
| Travelpayouts token client-side | Absent ✅ (only `FLIGHT_PROXY` URL + public marker) |
| `.gitignore` | `.env*`, `*.pem/.key/.p8`, `Peakly-Business-Plan.*`, office formats all covered ✅ |
| Supabase | anon key only (public-safe, RLS-gated) ✅ |
| Git remote | SSH (`git@github.com:j1mmychu/peakly.git`) — no PAT in any remote URL ✅ |
| SRI/CSP on CDN scripts | known-skipped 2026-05-04, not re-filed |

## 5. Performance & Invariants — ✅

| Check | Result |
|-------|--------|
| `app.jsx` | 13,021 lines / 652,180 bytes (~637KB raw, ~175KB gzip est.) |
| Brace balance | **5509 / 5509** ✅ |
| Venue count (eval bracket-walker, not grep) | **353** (130 skiing / 223 beach), **0 duplicate IDs** ✅ matches `scripts/.venue-baseline` (353) + the auto-push eval guard |
| GEAR_ITEMS | **0 — correct, intended state** (Jack's v1 cut) ✅ |
| Eager Supabase first-paint cost | Still live at `index.html:83–85` (~80KB gzip to anonymous visitors) — known-skipped; regenerated diff ready (see §9) |
| Unsplash `auto=format&q=75` | Still missing on photo URLs — known-skipped, re-flags at MAU > 100 / LCP regression |

## 6. Cost — unchanged

DigitalOcean droplet $6/mo (patched + rebooted 06-10, zero pending updates). All external APIs free-tier. Projection holds: 1K MAU ≈ $6/mo; 10K ≈ $6–31/mo (droplet upsize + possible Open-Meteo commercial plan ~€29/mo); 100K ≈ ~$49.

## 7. 🟡 NEW — local working tree diverged from origin (latent merge conflict on a report file)

**What:** the mounted working tree (HEAD `dcc65f8`) is **3 commits behind `origin/main`**, and is also carrying **uncommitted agent writes** that were never pushed:

- origin/main has 3 commits HEAD lacks — `8d4afef` PM v55, `837a963` Content, `ae5c4ef` DevOps (all dated 06-11). **Reports-only — diffstat touches `reports/{pm,content,devops}-report.md` and NOTHING in app.jsx/index.html/sw.js.**
- local uncommitted: `M CLAUDE.md`, `M reports/pm-report.md`, plus untracked `reports/inputs/devops-2026-06-11.md`, `reports/inputs/pm-2026-06-11.md`, `reports/ready-to-ship/eager-supabase-delete-2026-06-11.diff`.

**Why it matters:** both sides independently rewrote `reports/pm-report.md` (origin `8d4afef` vs the local `M`). `git merge-tree` confirms it's a *changed-in-both* file → **a plain `git pull` will conflict on `pm-report.md`.** `CLAUDE.md` is clean to commit (origin didn't touch it).

**Severity: LOW / Jack-only.** This is Open #11 (auto-push orphans scheduled-agent writes) manifesting — the 06-11 reports got committed on a different machine/session and pushed, while this tree accumulated its own report writes. It **cannot break the live site or the deploy** (origin's 3 commits are reports-only; app.jsx is byte-identical across HEAD and origin). It only blocks the reports tree from reconciling cleanly. Not re-filing as a code finding — it's a one-time reconcile + the standing fix already in CLAUDE.md #11.

**Fix (Jack, ~2 min):**

```bash
cd ~/peakly
git add CLAUDE.md reports/inputs/ reports/ready-to-ship/   # stage the clean stuff
git stash push reports/pm-report.md                         # park the conflicting file
git pull --rebase                                           # pulls the 3 reports-only commits cleanly
git stash pop                                               # reconcile pm-report.md by hand (keep origin's v55 body; it's newer)
git commit -am "reconcile: agent report writes 06-11→13"
git push
```

Standing prevention (already recommended in CLAUDE.md #11, still not installed): add the post-briefing cron so scheduled-agent writes auto-commit instead of piling up —

```
45 17 * * * cd ~/peakly && bash scripts/auto-push.sh
```

## 8. What breaks first at scale — unchanged from 06-11

With the proxy weather cache verified live (493 entries, serving), the client-side Open-Meteo throttle is no longer first in line — N users on the same venue collapse to 1 upstream call per coord per 2h. First break is now **Open-Meteo's free-tier daily ceiling as seen from the single VPS IP**: at 353 venues + marine for 223, full cache churn is ~5–7K upstream calls/day vs a ~10K/day non-commercial ceiling. A jump to ~500+ venues or a TTL cut crosses it. Cheap prevention: lengthen cache TTL to 3h (one constant) and budget Open-Meteo's ~€29/mo API plan into the 10K-MAU line. The 1GB droplet itself is fine well past 10K MAU.

## 9. Diffs ready to apply

- `reports/ready-to-ship/eager-supabase-delete-2026-06-11.diff` — removes the eager Supabase `<script>` (index.html:83–85); regenerated 06-11 because the 05-08 diff is corrupt; `git apply --check` clean. ~80KB gzip off anonymous first paint. (Replaces, does not re-file, the known-skipped entry. When applying, also `git rm` the corrupt `eager-supabase-delete-2026-05-08.diff`.)

## 10. Two-strikes / not re-filed this run

- **PAT expiry** — permanently retired (no live consumer; resolved 06-10). Not flagged.
- **GEAR_ITEMS = 0** — correct intended state, never a leak. Not flagged.
- **Eager Supabase / SRI / CSP / Unsplash params / UptimeRobot / APNS / VPS redeploy** — all known-skipped, conditions unchanged.
- **Localhost CORS origins in `server/proxy.js`** — flagged 06-09 + 06-10, dropped 06-11; LOW severity (lets a local dev server burn proxy quota, no data exposure). Bundle the `NODE_ENV` gate into the next VPS SSH session. Not escalating.

## Action items

| # | Item | Time | Owner |
|---|------|------|-------|
| 1 | Reconcile local↔origin reports tree (§7 commands) — clears the latent `pm-report.md` conflict | 2 min | Jack |
| 2 | Install the `45 17 * * *` auto-push cron so scheduled-agent writes stop orphaning (CLAUDE.md #11) | 2 min | Jack |
| 3 | One-time paste of `server/sql/delete-account.sql` into Supabase SQL editor (App Store 5.1.1(v)) | 2 min | Jack |
| 4 | Apply `eager-supabase-delete-2026-06-11.diff` (+ `git rm` the corrupt 05-08 diff) | 30 sec | Jack |
