# Peakly PM Report — 2026-04-16 (v24)

**Filed by:** Product Manager agent  
**Status:** 7-day code freeze. TP_MARKER Day 23. Two briefing claims are stale/wrong — corrected below.

---

## Shipped Since Last Report (2026-04-09 → 2026-04-16)

| Commit | What | Right call? |
|--------|------|-------------|
| `1db7079` (Apr 9) | Inter-batch 1s delay for Open-Meteo throttling + SW cache bump | ✅ Correct — prevents rate-limit crashes |
| `3129564` (Apr 9) | Content report merge (98 lines, app.jsx change = 4 lines only) | ⚠️ 4 lines of real code inside a report commit is sloppy |
| — | **Nothing shipped Apr 10–16** | ❌ 7-day code freeze after a 6-day gap on Apr 2-8 |

**Two weeks of combined inactivity in April.** Both gaps landed after a PM report warning they would happen. The pattern is confirmed: PM identifies priorities, priorities don't ship.

---

## Briefing Corrections

The session brief contained two stale claims. Correcting the record:

**"Peakly Pro price showing $9/mo — should be $79/yr"**  
WRONG. There is zero Pro pricing UI in the live app. All Pro UI was removed 2026-03-26 (CLAUDE.md line 79, 262, 298, 315, 369). The "Peakly Pro $79/yr" entry in the revenue model is waitlist-only — not displayed to users. This is a non-issue.

**"Sentry DSN empty"**  
WRONG. Sentry is initialized at app.jsx:7–8 with a real ingest DSN. Error monitoring is live. Non-issue.

---

## Bug Triage — April 16

| Bug | Severity | Status | Days Open |
|-----|----------|--------|-----------|
| **TP_MARKER = "YOUR_TP_MARKER"** | **P0** | ❌ STILL UNSET | **Day 23** |
| **Email capture → `alert()` void** | **P1** | ❌ No backend POST. Every signup lost. | Day 30+ |
| **Cache buster stale at `?v=20260409a`** | **P2** | ⚠️ 7 days old, not critical until next deploy | Day 7 |
| **app.jsx = 11,000 lines (Babel parse lag on mobile)** | **P1** | ❌ Getting worse. No VENUES split. | Day 14+ |
| **Sentry DSN** | ~~P1~~ | ✅ **CLOSED — was already set** | — |
| **Pro pricing discrepancy** | ~~P1~~ | ✅ **CLOSED — no Pro UI exists in live app** | — |

**Net: 2 bugs closed (false alarms), 4 real bugs still open, zero fixed this week.**

---

## Known Blockers

| Blocker | Owner | Status |
|---------|-------|--------|
| **TP_MARKER placeholder** | Jack — tp.media → Markers → copy ID → app.jsx:5316 → push | **P0. Day 23. All flight affiliate revenue $0 since March 24.** |
| **Email capture no backend** | Dev — line 7214: replace `alert("You're on the list! 🎉")` with POST to `https://peakly-api.duckdns.org/api/waitlist` | P1. Backend already built (proxy.js). Just not wired to the form. |
| **app.jsx 11K lines** | Dev — extract VENUES array to venues.json + async fetch on init | P1. Mobile cold-start lag. Worsens with each venue add. |
| **LLC approval** | External | Unblocks REI (+$6.16 RPM), Backcountry, GetYourGuide, Stripe |

---

## This Week's Top 3 Priorities

### 1. JACK: TP_MARKER. Day 23. $0 in affiliate revenue since March 24.

`tp.media` → Markers → copy your marker ID → replace `"YOUR_TP_MARKER"` at `app.jsx:5316` → bump cache buster in `index.html` → push.

This is the only P0. It has been in every PM report since v13. The backend is live, the flight links fire, the affiliate attribution is broken. 5 minutes of Jack's time. No other fix matters more.

**DECISION 1: SHIP. Jack. This session if possible.**

### 2. DEV: Wire email capture to VPS backend (30 minutes)

The `/api/waitlist` endpoint already exists in `server/proxy.js` (shipped 2026-04-10 per CLAUDE.md). The onboarding form collects email at `app.jsx:9039`. The explore banner form is at `app.jsx:7214`. Both currently fire `alert()` and discard the email.

Replace both `alert()` calls with:
```js
fetch("https://peakly-api.duckdns.org/api/waitlist", {
  method:"POST",
  headers:{"Content-Type":"application/json"},
  body: JSON.stringify({ email, name: profile?.name || "" })
}).catch(()=>{});
```
Show inline success text instead of `alert()`. That's the entire implementation. 30 minutes max.

**DECISION 2: SHIP this week. One dev session. Block nothing else on it.**

### 3. DEFER: Everything else until TP_MARKER + email capture are shipped

No new features, no venue expansion, no SEO work until the two items above are done. The opportunity cost of shipping a feature while TP_MARKER sits unset is real: every new user who clicks a flight link generates $0.

**DECISION 3: Feature freeze until P0 + P1 are closed.**

---

## Features REJECTED This Week

| Feature | Decision | Reason |
|---------|----------|--------|
| **JSON-LD structured data** | **DEFER** | SEO compound interest, but zero impact without traffic. Fix monetization first. |
| **Static H1 fallback** | **DEFER** | Same reasoning. Not the constraint. |
| **Venue deep links** | **DEFER** | Decided post-Reddit launch. Standing decision. |
| **venues.json split** | **DEFER one more week** | Right call, but not before TP_MARKER + email. |
| **iOS App Store** | **DEFER to 500 validated users** | $99 + 4-week review. Validate PMF first. |
| **Strike alerts server polling** | **DEFER to 100 subscribers** | Build the list before building the push. |
| **Onboarding scoring explanation** | **DEFER** | Nice UX. Not blocking 100K users. |

---

## Success Criteria

| Metric | Current (base case) | 8K users (upside) |
|--------|--------------------|--------------------|
| Flight affiliate revenue | $0 (TP_MARKER broken) | Flowing by Apr 17 |
| Email list size | 0 captured (alert void) | 50+ via backend wiring |
| Mobile cold-start | ~5s Babel parse lag | <2s via venues.json split |
| Retention trigger | None (no emails, no push) | Weekly alert fires → users return |
| Cache freshness | Stale 7d | Bumped with every code deploy |

**What has to be true for 8K, not 5K:**  
Two things only. TP_MARKER set (revenue per visitor). Email backend wired (re-engagement). Everything else is optimization. These two items are both under 30 minutes each and have been identified for 3+ weeks. The 8K path is not blocked by product strategy — it is blocked by execution.

---

## The Product Risk Nobody Is Talking About

**The agent team is a reporting machine with no enforcement layer.**

In 24 PM reports, TP_MARKER has been P0 in every single one. It has never been set. The reports are correct. The priorities are right. The bugs are real. The issue is that there is no mechanism between "PM files report" and "Jack ships the fix."

This is not a prioritization problem. It is a process problem. A single session where Jack sets TP_MARKER and a dev session wires the email backend would close the two most important open items in the entire backlog. Combined time: under 45 minutes.

The risk is that another 24 reports get filed, the gaps grow to Day 47 and Day 53, and the Reddit traffic window closes permanently before the foundation is solid.

**The fix is not another report. The fix is Jack setting TP_MARKER today.**

---

*PM agent v24 — 2026-04-16*  
*Filed for: Jack (jjciluzzi@gmail.com)*  
*Next milestone: TP_MARKER in git by April 17. Email backend wired by April 18.*
