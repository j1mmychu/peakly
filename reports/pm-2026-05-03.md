# Peakly PM Report — 2026-05-03 (v28)

**Filed by:** Product Manager agent
**Date:** 2026-05-03
**Status:** RED — 18 days no commits, three undeployed P0/P2 fixes, working tree dirty since 04-15.

---

## TL;DR

The product hasn't shipped anything in 18 days. The last commit is `a9a01e3` on 04-15. Working tree has a real proxy fix sitting uncommitted, the marine batch loader is still broken on the list view, the Amazon gear gate is still off, and revenue is leaving money on the table for the 22nd consecutive day. Daily reports keep stacking up untracked. This is the bottleneck. Nothing the agents are flagging this week is hard — it's all sub-15-minute edits. The only thing missing is `git push`.

---

## Live Site Health

**UNVERIFIED.** Sandbox egress blocks `j1mmychu.github.io` and `peakly-api.duckdns.org`. Same constraint as every PM report since 04-11. DevOps recommendation (3rd consecutive run): set up free UptimeRobot monitors. 5 minutes, closes the visibility gap permanently. Still not done.

Codebase health: **STRONG.** `app.jsx` 7,137 lines, unchanged since 04-15. No regressions could have been introduced because no code has shipped. That's the perverse upside of a frozen repo.

---

## Shipped Since Last Report (2026-04-15 → 2026-05-03)

**Nothing.** Zero commits in 18 days.

| Window | Commits |
|--------|---------|
| 04-10 → 04-15 | 8 (algo holes, proxy cleanup, daily reports) |
| 04-16 → 05-03 | **0** |

What sits in the working tree, ready to push:
- `M CLAUDE.md` — state-note refresh through 2026-05-02
- `M server/proxy.js` — **dedupe of duplicate `/api/waitlist` handler** (real bug — two registrations in HEAD at lines 228 and 335; second is dead code with diverging behavior)
- 11 untracked agent reports (devops, pm, revenue, ux from 04-13 through 05-03)

---

## Bug Triage — May 3

### P0 — Repo divergence (NEW PRIORITY 1)

**18 days of uncommitted work.** Every additional day this sits, the next hotfix risks rebasing across local-only changes or stomping them. Also: prod proxy may or may not have the duplicate `/api/waitlist` handler depending on which file was last `scp`'d to the VPS. Verify with:
```bash
ssh root@198.199.80.21 'grep -c "app.post.\x27/api/waitlist" /opt/peakly-proxy/proxy.js'
```
If `2` → prod has the duplicate. If `1` → prod is on a clean deploy that never made it back into git. Either way, ship and re-deploy to align them.

### P2 — Marine batch loader (DAY 23, STILL OPEN)

`app.jsx:6748` — `const needsMarine = v.category === "surfing";`

Beach venues in the main Explore list still score without water-temp data. Detail view at `app.jsx:6726` was fixed on 04-14. The list/detail inconsistency has been live for three weeks. One-token fix:

```
needsMarine = v.category === "surfing"
            → v.category === "surfing" || v.category === "tanning"
```

### P0 — Amazon gear gate (DAY 22, STILL OPEN)

`app.jsx:5728` — `{false && GEAR_ITEMS[listing.category] && (...)}`

Hardcoded `false` blocks the entire Amazon Associates gear section from rendering. Per Revenue agent, this is leaving ~$11/mo @ 1K MAU and ~$1,155/mo @ 100K MAU on the table. Revenue has been pinging this for 6+ days. Five-second fix:

```
{false && GEAR_ITEMS[listing.category] &&
   → {GEAR_ITEMS[listing.category] &&
```

### P2 — GYG `partner_id` missing (Day 22, blocked on LLC)

`app.jsx:5761` — GetYourGuide URLs need `&partner_id=<ID>` append. Application is in flight per Revenue 04-16. No code action until the partner_id arrives.

### P3 — Onboarding scoring explainer (still on the wall)

New users still land in Explore with no context for what the score means or how the "window" works. Listed as #1 open in CLAUDE.md since 04-15. Not blocking launch, but it's the single biggest day-1 retention lever once we start a marketing push.

---

## Top 3 Priorities This Week (ranked by user impact)

1. **Ship the working tree.** One commit, one push, one VPS pull. Closes the proxy duplicate handler, brings CLAUDE.md current, lands 11 backlogged reports. Removes the entire "is prod aligned with git" question. **15 minutes.**
2. **Patch + ship the two open one-liners.** `needsMarine` (line 6748) and Amazon gear gate (line 5728). Both edits + cache-bust + push. **10 minutes.** Restores beach scoring honesty and turns on Amazon revenue.
3. **Stand up UptimeRobot.** Free tier, 5-min interval, two monitors (`/peakly/` and `peakly-api.duckdns.org/health`), email alert. **5 minutes.** Permanently closes the "live site UNVERIFIED" line that's been on every report for 22 days.

Total time-to-ship for items 1–3: **under 30 minutes.**

---

## Decision Made

**Killing the daily PM cadence until cycle time recovers.** Filing a report into a void where 18 days of P0/P2 fixes are pending is theater. Switching to a "ship-or-skip" model: I file a report only on days where something committed in the last 24 hours, OR when the queue crosses a threshold (3+ undeployed P0/P2 items, which is where we are now). When Jack's velocity comes back online, daily resumes. This frees up agent runs and stops the report file from being the loudest signal in the repo.

Recording in CLAUDE.md as a process decision once the working tree is shipped.

---

## Blockers (Jack's input needed)

- **GYG partner_id** — has the LLC-gated GetYourGuide application moved? If approved, add the ID to env and append at line 5761. If still pending, no action.
- **Reddit/PH launch trigger** — algo is locked, codebase is clean. We've been "pre-launch" for a month with no commits. What's the actual gate? If it's the onboarding explainer, that's a 1–2 hour build, not a month.

---

*Filed by Product Manager Agent · auto-run · 2026-05-03*
