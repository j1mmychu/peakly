# DevOps Report — 2026-04-08

## System Status: UNABLE TO VERIFY (sandbox egress blocked)

Health checks could not be performed from this environment — network egress is blocked for all external domains (GitHub Pages, peakly-api.duckdns.org, Open-Meteo). Jack should verify manually or set up an external monitor (UptimeRobot is already configured per CLAUDE.md).

## HTTPS Status: DONE

Caddy + Let's Encrypt is live on peakly-api.duckdns.org. All client-side URLs use HTTPS. No mixed content issues detected in app.jsx.

## Performance

- **app.jsx**: 10,993 lines / 1.95 MB raw JSX
- **Venues**: 3,715 entries loaded upfront in the VENUES constant
- **Batching**: Good — initial load fetches top 100 venues, weather batched at 50/batch
- **Pagination**: 30 venues rendered initially, "Show More" button for incremental loading
- **No N² loops detected** — scoring is O(1) per venue, sorting is O(n log n)
- **Minor concern**: Pull-to-refresh touch handler lacks debouncing, could cause jank on low-end devices
- **No 429 handling on fetchWeather** (P1 from CLAUDE.md still open) — HTTP errors throw without retry or backoff

## Cache Status

| Asset | Version | Date |
|-------|---------|------|
| Cache buster (index.html) | v=20260331a | March 31 |
| Service worker (sw.js) | peakly-20260330 | March 30 |

**Cache buster is 8 days stale** (last bumped March 31, today is April 8). Any code changes pushed since then won't reach users with cached SW. Needs bump to `v=20260408a` and SW to `peakly-v15`.

**SW/app version skew**: SW cache name (20260330) is 1 day older than the app cache buster (20260331a). Minor but worth syncing on next deploy.

## Security: CLEAN

- No hardcoded API tokens, secrets, or passwords in app.jsx or index.html
- Travelpayouts token properly isolated in server-side environment variable (server/proxy.js line 9)
- CORS whitelist configured on proxy server
- All external URLs use HTTPS
- `TP_MARKER` is still set to placeholder `"YOUR_TP_MARKER"` — **this means $0 earned on every flight click**. P0 issue, still open since initial flagging.

## Open P1 Issues (DevOps-relevant)

1. **TP_MARKER placeholder** — $0 revenue on all flight clicks. Jack action needed, 5 minutes.
2. **Cache buster stale 8 days** — v=20260331a needs bump to v=20260408a.
3. **fetchWeather no 429 handling** — will break under load. Needs exponential backoff + retry.
4. **Email capture has no backend** — signups are lost (alert() only).

## Decision Made

**Cache buster bump is overdue.** Any fixes shipped since March 31 are invisible to returning users. Recommending immediate bump to `v=20260408a` and SW to `peakly-20260408`. This is a 2-minute fix that unblocks all recent work.

## Cost Estimate

| Service | Monthly |
|---------|---------|
| DigitalOcean VPS (1GB) | $6 |
| DuckDNS domain | $0 |
| GitHub Pages | $0 |
| Plausible analytics | $9 |
| Sentry (free tier) | $0 |
| UptimeRobot (free tier) | $0 |
| **Total** | **~$15/mo** |
