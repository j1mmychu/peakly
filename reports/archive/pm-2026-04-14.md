# Peakly PM Report — 2026-04-14 (v26)

**Filed by:** Product Manager agent
**Date:** 2026-04-14
**Status:** YELLOW — code is healthy, shipping velocity is stalled.

---

## TL;DR

Zero commits in the last 48 hours. The P2 from yesterday (one-line beach marine fix) is still sitting there. UX agent has filed "nothing landed" two days running. Revenue agent has flagged the same 3 blockers for 3 days. The bugs are fixed; the backlog isn't moving.

---

## Live Site Health

**Can't verify.** Egress blocked from this sandbox for both `j1mmychu.github.io` and `peakly-api.duckdns.org`. Same as v25. Jack: Settings → Capabilities → allowlist these two domains or I'm permanently flying blind on uptime. This is now a repeat blocker.

Codebase health: **STRONG**. `app.jsx` is 7,038 lines, clean parse, no new crash vectors introduced.

---

## Shipped Since Last Report (2026-04-13 → 2026-04-14)

| Commit | What |
|--------|------|
| — | **Nothing.** Last real commit was `69e555d` on Apr 12 (7 algorithm holes). `29e6719` on Apr 12 was the merge. |

48 hours dark. Three agent reports filed in that window (PM v25, Revenue, UX). Zero actions taken on any of them.

---

## Bug Triage — April 14

### P2 STILL OPEN (called out in v25, unfixed 24h later)

| Bug | Line | Fix |
|-----|------|-----|
| **Beach marine data not fetched in batch loader** | `app.jsx:6649` | `const needsMarine = v.category === "surfing";` → `const needsMarine = v.category === "surfing" \|\| v.category === "tanning";` |

Verified today at line 6649 — unchanged. This is a literal one-token fix. Beach venues show inconsistent water-temp-aware scores between Explore list (no marine) and detail view (marine). 77 venues impacted.

### Cross-Agent Bug Roll-Up

Pulling the open items flagged by other agents today:

**From UX (v apr-13, design score 4/10):**
- Hero overlay 0.55–0.6 still too dark (called out Apr 11, 12, 13) — washes out photos
- Touch targets below 44px on share (32), heart (36), nav pills (~32) — iOS HIG violation
- Border-radius zoo (18 different values) across the app
- Loading hero has no shimmer — dead grey box during fetch
- Condition loading text at `#aaa` is below AA contrast on white
- Dead `icon` emoji field still declared in VENUES even after emoji strip

**From Revenue (day 3 of same blockers):**
- Gear affiliate gate at `app.jsx:5629` still `{false && ...}` — Amazon is live but hidden
- GetYourGuide URLs at `app.jsx:5662` still have no `partner_id` — zero commission on every experience click
- No `experience_click` or `insurance_click` Plausible events — can't measure two of the six touchpoints

**From backlog (tasks/todo.md):** Still drifted. Calls out "Add Sentry DSN" (done), "Dark mode toggle" (cut), "Add venue photos" (done), "Expand LOCAL_TIPS" (pruned away). Every agent that reads this file wastes cycles.

---

## This Week's Top 3 — Reordered for Velocity

The v25 priorities weren't wrong; they just didn't ship. Re-racking by cost-to-execute so we actually move:

### 1. **Ship the stalled one-liners today.** (30 min total)

Three changes, each under 5 lines:

- `app.jsx:6649` — add `|| v.category === "tanning"` (P2 scoring consistency)
- `app.jsx:5629` — flip `{false && ...}` to `{GEAR_ITEMS[listing.category] && ...}` (unlocks Amazon gear stream)
- `app.jsx:5662` — append `&partner_id=<PEAKLY_GYG_ID>` to GYG URLs (unlocks experience commission)

**Decision: SHIP all three in a single commit today.** These have been "next up" in two separate agent reports for 72 hours. Either execute or explicitly cut.

### 2. **Onboarding score explainer.** (still #1 on v25, still open)

New users see "72" with no context. This is the entire value prop rendered as an un-labeled integer. Scope: a single info dot next to the score that opens a short explainer sheet. Under 50 lines of JSX. No full onboarding flow — just one tooltip.

**Decision: Ship this week before Reddit launch. Non-negotiable for first impression.**

### 3. **Kill the stale backlog.**

`tasks/todo.md` is actively misleading. Delete completed items, remove cut items, sync to CLAUDE.md "open" list. 10-minute cleanup. Every agent run gets cheaper after this.

**Decision: SHIP this week.**

---

## Features REJECTED / DEFERRED

| Feature | Decision | Reasoning |
|---------|----------|-----------|
| Venue expansion (50+ new spots) | **DEFER** (unchanged from v25) | 231 is enough for launch; cold-load cost is real |
| Strike alerts server polling | **DEFER** (unchanged from v25) | No user demand signal |
| Dark mode | **CUT** (long-standing decision in CLAUDE.md) | Remove from backlog file |
| Hotel deep links per venue | **DEFER** | Post-1K users. Revenue opt before PMF. |
| Premium tier launch | **DEFER** | Waitlist only, validate demand first |

---

## Product Decision Today

**Velocity is now the #1 product risk, not bugs.** The code is honest, the scoring is defensible, the proxy is hardened. What's killing us is that three agents are filing well-scoped findings into a repo that doesn't move. Two options, and I'm picking:

**Option A (rejected):** Keep agent cadence, keep filing reports, hope Jack has time.
**Option B (shipped):** Every daily agent report gets a companion 15-min "execute top-1 item" block. If Jack can't commit, the item escalates to CLAUDE.md's "Broken/Open" list so it's unavoidable at next session start.

**Decision: Go with B.** Starting today, v25's P2 (beach marine fix) is being promoted into CLAUDE.md's "What's Broken / Open" list so it cannot be ignored at next session boot.

---

## Codebase Vitals

| Metric | Value | Δ vs v25 |
|--------|-------|----------|
| `app.jsx` lines | 7,038 | 0 |
| VENUES count | 231 | 0 |
| Categories active | 3 + "all" | 0 |
| Last deploy commit | `29e6719` (Apr 12) | unchanged |
| Days since last code commit | **2** | +1 |
| Open P2s called out in prior report | 1 | still open |

---

## Blockers for Jack

1. **Egress allowlist for live-site health checks.** Two domains: `j1mmychu.github.io`, `peakly-api.duckdns.org`. Settings → Capabilities. One-time 30-second config.
2. **GYG partner ID.** If we have one from the LLC pipeline, drop it; if not, confirm we're stuck until LLC approves.

Nothing else external. The rest is execution.

---

## 90-Day Projection

Holding v25's **8K users** projection. The product is launch-ready at a code level. What closes the gap to 8K is:

- Ship the 3 one-liners today (revenue + score consistency)
- Ship onboarding explainer this week (retention)
- Book the Reddit launch window (distribution)

If none of those happen in the next 7 days, downgrade back to 5K.
