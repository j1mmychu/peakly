# Peakly PM Report — 2026-04-13 (v24)

**Filed by:** Product Manager agent  
**Date:** 2026-04-13  
**Status:** TP_MARKER Day 20. Email capture still dead. VENUES array at 3,715 entries — content agent has undone the April 10 scope decision.

---

## Shipped Since Last Report (2026-04-08 → 2026-04-13)

| Commit | Date | What | Right call? |
|--------|------|------|-------------|
| `1db7079` | Apr 9 | Push notification icon fix (was pointing at manifest.json) | ✅ Correct. Broken push icons are a silent credibility killer. |
| `1db7079` | Apr 9 | SW cache bump → `peakly-20260409` | ✅ Correct. |
| `1db7079` | Apr 9 | 1s inter-batch delay in fetchInitialWeather | ✅ Correct. Throttles Open-Meteo within free tier. |
| `1db7079` | Apr 9 | Cache buster → `?v=20260409a` | ✅ Correct. |

**Zero code shipped April 10–13.** Three days of inactivity. TP_MARKER is on Day 20 with no update.

### Were these the right things to build?

Yes — all four changes were correct and low-risk. But they're maintenance, not growth. The push notification fix only matters if alerts are being fired. Alerts fire when a venue hits target score. The backend polling worker that fires those alerts hasn't been built. So we shipped icons for a feature whose backend doesn't exist. That's P3 work while P0 fires burn.

---

## Bug Triage — April 13

| Bug | Severity | Status | Days Open |
|-----|----------|--------|-----------|
| **TP_MARKER = "YOUR_TP_MARKER"** | **P0** | ❌ STILL UNSET | **Day 20** |
| **Email capture uses `alert()` — no backend POST** | **P1** | ❌ Not fixed. CLAUDE.md says fixed. Code disagrees. `app.jsx:7214` | Day 20+ |
| **VENUES array has 3,715 entries** | **P1** | ❌ Content agent blew past the April 10 scope cut. CLAUDE.md says 231. Reality is 3,715. 1,614 are non-launch categories. | Regressed Apr 9 |
| **Cache buster stale** | **P3** | `?v=20260409a` last bumped Apr 9. Should bump on every code deploy. | Day 4 |

### Cleared — No Longer Bugs

| Item | Status |
|------|--------|
| Sentry DSN empty | ✅ CLEARED. Real DSN at `app.jsx:7`. Monitoring live. |
| React CDN unpinned | ✅ CLEARED. `index.html:80-81` — pinned to 18.3.1. |
| JSON-LD structured data missing | ✅ CLEARED. Present in `index.html:34-50`. |
| Peakly Pro showing $9/mo | ✅ NOT A BUG. Pro UI removed 2026-03-26. Replaced with email waitlist. Per `reports/revenue-report.md`. $79/yr decision stands for when Pro ships. No action needed. |

### Deep Dive: The Email Capture Lie

CLAUDE.md "Recently Fixed (2026-04-10)" states:
> ✅ Email capture: real POST to `/api/waitlist`, inline status feedback (no more `alert()`)

`app.jsx:7214` says:
```js
alert("You're on the list! 🎉");
```

No POST. No backend. Every email a Reddit visitor typed since launch has been discarded. This fix was listed as done and it wasn't done. CLAUDE.md needs to be corrected and the fix actually shipped.

### Deep Dive: The VENUES Scope Regression

CLAUDE.md "Recently Fixed (2026-04-10)" states:
> ✅ VENUES scaled 3,726 → 257 (unique-photo dedupe) → 231 (launch cats only)

Current `app.jsx:306` VENUES array: **3,715 entries**. Breakdown:
- Launch categories (ski/surf/tanning): **2,112 entries** — 9x the target 231
- Non-launch categories (mtb/climbing/kite/fishing/paraglide/hiking/diving): **1,614 entries** — all deferred

The content agent has been adding venues in every session. The April 10 cleanup was either never done or immediately overwritten. We're running a 2MB Babel-transpiled file on every cold load. Mobile users on 4G get a 5-10s parse time before the app renders. Every second is users leaving.

---

## Known Blockers

| Blocker | Owner | ETA |
|---------|-------|-----|
| **TP_MARKER placeholder** | Jack — `tp.media` → Markers → copy ID → `app.jsx:5316` → push | **TODAY. Day 20. No excuse.** |
| **Email capture dead** | Dev — `app.jsx:7214`, replace `alert()` with `fetch` POST to `/api/waitlist` | **This week. 15 minutes.** |
| **VENUES scope regression** | Dev — delete 1,614 non-launch entries; trim launch cats to top ~231 | **This week. Freeze content agent additions.** |
| **LLC approval** | External | Unblocks REI (+$6.16 RPM), Backcountry, GetYourGuide, Stripe |

---

## This Week's Top 3 Priorities Only

### 1. JACK: TP_MARKER. Day 20. $0 affiliate revenue on flight clicks for three weeks.

`tp.media` → Programs → Aviasales → Markers → copy Marker ID → `app.jsx:5316` → push.

If Reddit brought 500 visitors and 10% clicked a flight link at 2% conversion → ~$12 in lost commissions. Not catastrophic individually. But Day 20 becomes Day 30 becomes Day 60. The fix is 5 minutes. **Do it before reading further.**

**Decision: SHIP. Jack. Today.**

### 2. DEV: Fix email capture. 15 minutes.

`app.jsx:7214`. The VPS `/api/waitlist` endpoint exists and works. The client call doesn't.

Replace:
```js
alert("You're on the list! 🎉");
```

With a `fetch` POST to `https://peakly-api.duckdns.org/api/waitlist` with `{email}`, then update inline UI state to "You're on the list!" — no toast library, no modal, just `useState`. Bump cache buster after.

**Decision: SHIP. This week.**

### 3. DEV: Trim VENUES to launch scope. Critical for mobile performance.

Delete all 1,614 non-launch category venues from `app.jsx:306` VENUES array. Then trim the 2,112 launch venues down to top ~231 (77 surfing, 77 skiing, 77 tanning). This single change takes app.jsx from 2MB toward ~800KB, cutting Babel parse time by ~60% on mobile.

Add to content agent task prompt: **"Venue scope is frozen. Do NOT add to VENUES array until post-launch decision."**

**Decision: SHIP this week. Freeze content agent venue additions immediately.**

---

## Features REJECTED This Week

| Feature | Decision | Reasoning |
|---------|----------|-----------|
| **Strike alerts backend polling** | **DEFER** | Email pipeline must work first. Alerts notify via email. Email is broken. Fix in order. |
| **iOS App Store submission** | **DEFER to 500 users** | $99/yr before demand is proven is waste. Web-first. |
| **Google Play Store** | **DEFER to 500 users** | Same. $25 + time. Web-first. |
| **JSON-LD per-venue structured data** | **CUT until post-Reddit** | WebSite + WebApplication schema is live and good enough. Per-venue JSON-LD is Phase 2 SEO. |
| **Dark mode** | **CUT permanently** | Fourth consecutive report. Still no. Not in the next 12 months. |
| **Trips + Wishlists tabs** | **DEFER to 1K users** | Nav is clean. Core flow first. |

---

## Success Criteria

| Metric | 5K users (base) | 8K users (upside) |
|--------|----------------|-------------------|
| TP_MARKER set + commissions flowing | Required | Required |
| Email capture working + 100+ addresses | Required | Required |
| Second Reddit post | Optional | **Required** |
| VENUES trimmed → app.jsx < 1MB → mobile load < 2s | Helpful | Required |
| Week-2 retention via email re-engagement | 5% return | 20% return |

### What has to be true for 8K, not 5K?

One thing: **a second Reddit post before the April ski season fully closes.** r/skiing has 3+ weeks of season traffic. r/solotravel is year-round. A post framed around ski + beach alignment ("ski in the morning, beach tomorrow — Peakly finds when both are cheap") is the content. But TP_MARKER must be live before that post goes up. Non-negotiable.

**90-day projection:** 5K-8K achievable IF second post happens this week and email re-engagement lands. No second post + continued TP_MARKER drought → tracks to 2K-3K and flat-lines.

---

## One Product Risk Nobody Is Talking About

**The content agent is autonomously undoing product scope decisions.**

The April 10 VENUES trim (3,726 → 231) was overwritten within days. CLAUDE.md says 231 venues. The app has 3,715. The content agent doesn't know that "more venues = worse mobile performance = lower conversion." It's optimizing for "more content" because nobody told it the current constraint is load time, not catalog size.

This will keep happening every session unless the content agent's task prompt is updated with an explicit freeze. The reports are filed, nobody reads them, the agent adds more venues.

**Fix:** Add one line to `tasks/agents/` content prompt: "Venue scope frozen at 231 entries. Do not add to VENUES array. Venue work limited to: fixing `venue.facing` data for surfing venues missing it." Until that constraint is explicit, the drift continues.

---

## Decisions Made This Report

| Date | Decision |
|------|----------|
| 2026-04-13 | **TP_MARKER: P0. Day 20. Jack ships before end of day.** |
| 2026-04-13 | **Email capture: SHIP. `app.jsx:7214`. Replace `alert()` with POST. This week.** |
| 2026-04-13 | **VENUES: freeze at ~231 launch entries. Delete 1,614 non-launch. Content agent prompt must reflect this.** |
| 2026-04-13 | **Peakly Pro $9/mo: NOT a bug. Pro UI removed 2026-03-26. $79/yr locked for future. No action.** |
| 2026-04-13 | **Second Reddit post: r/skiing or r/solotravel. This week. TP_MARKER must be live first.** |
| 2026-04-13 | **iOS/Android store submissions: DEFER to 500 users.** |

---

## Action Checklist

- [ ] **Jack:** Copy TP_MARKER from `tp.media` → `app.jsx:5316` → push (5 min)
- [ ] **Dev:** `app.jsx:7214` — replace `alert()` with POST to `/api/waitlist` + inline success state (15 min)
- [ ] **Dev:** Trim VENUES to ~231 launch-scope only (60 min)
- [ ] **Dev/Jack:** Update content agent prompt to freeze venue additions
- [ ] **Dev:** Bump cache buster `index.html:346` after each deploy
- [ ] **Jack:** Second Reddit post — after TP_MARKER confirmed live

---

*Next report: 2026-04-14 | Filed by PM agent (v24)*
